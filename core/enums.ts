export enum UserState {
    start = "start",
    chat = "chat",
    awaiting_project_name = "awaiting_project_name",
    awaiting_repo_url = "awaiting_repo_url",
    awaiting_command = "awaiting_command",
    awaiting_chat_message = "awaiting_chat_message",
    awaiting_new_folder_name = "awaiting_new_folder_name",
}